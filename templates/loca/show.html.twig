{% extends 'base.html.twig' %}

{% block title %}Locus{% endblock %}

{% block body %}
    <h1>Locus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ locus.id }}</td>
            </tr>
            <tr>
                <th>Achtung</th>
                <td>{{ locus.achtung }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ locus.name }}</td>
            </tr>
            <tr>
                <th>Rating</th>
                <td>{{ locus.rating }}</td>
            </tr>
            <tr>
                <th>IsFavorite</th>
                <td>{{ locus.isFavorite ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ locus.address }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ locus.country }}</td>
            </tr>
            <tr>
                <th>Kind</th>
                <td>{{ locus.kind }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ locus.description }}</td>
            </tr>
            <tr>
                <th>CoordinatesExact</th>
                <td>{{ locus.coordinatesExact }}</td>
            </tr>
            <tr>
                <th>CoordinatesGeneric</th>
                <td>{{ locus.coordinatesGeneric }}</td>
            </tr>
            <tr>
                <th>Web</th>
                <td>{{ locus.web }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_loca_index') }}">back to list</a>

    <a href="{{ path('app_loca_edit', {'id': locus.id}) }}">edit</a>

    {{ include('loca/_delete_form.html.twig') }}
{% endblock %}
